spring.application.name=starpeople

# 1. Configura��o do Servidor Web (Tomcat)
server.port=8080

# 2. Configura��o da Conex�o com SQL Server
# Explica��o: jdbc:sqlserver://[Onde];databaseName=[Qual Banco];[Seguran�a]
spring.datasource.url=${DB_URL}

# Usu�rio e Senha
spring.datasource.username=${DB_USER}
spring.datasource.password=${DB_PASSWORD}

# Driver (O Motorista)
spring.datasource.driverClassName=com.microsoft.sqlserver.jdbc.SQLServerDriver

# 3. Configura��o do JPA / Hibernate (O Tradutor Java -> SQL)
# Mostra o SQL no console (�til para aprender)
spring.jpa.show-sql=true
# Formata o SQL para ficar bonito de ler
spring.jpa.properties.hibernate.format_sql=true
# Diz qual "sotaque" de SQL usar (SQL Server)
spring.jpa.database-platform=org.hibernate.dialect.SQLServerDialect

# Estrat�gia de Tabelas:
# 'update': Mant�m os dados e atualiza a estrutura se voc� mudar o Java.
# 'validate': S� confere se est� igual.
# 'none': N�o faz nada.
spring.jpa.hibernate.ddl-auto=update

# (Opcional) show-sql desligado para n�o poluir o log com milhares de selects
spring.jpa.show-sql=false


api.security.token.secret=!62yAAMN9hp*pk$xPXwqaJponw5PPD1XStb*SYz^