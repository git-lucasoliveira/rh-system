# --- PERFIL DE PRODU��O (Seguran�a M�xima) ---

# Porta (opcional - algumas clouds usam ${PORT})
server.port=${PORT:8080}

# Nome da Aplica��o
spring.application.name=starpeople-prod

# Conex�o com Banco de Dados (L� das vari�veis do servidor - NUVEM)
# N�o colocamos senhas aqui! O servidor vai injetar estes valores.
spring.datasource.url=${DB_URL}
spring.datasource.username=${DB_USER}
spring.datasource.password=${DB_PASSWORD}
spring.datasource.driverClassName=com.microsoft.sqlserver.jdbc.SQLServerDriver

# JPA / Hibernate (Em produ��o, NUNCA use 'update')
# 'validate' garante que o Java n�o estraga o banco se houver diferen�as
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=false
spring.jpa.database-platform=org.hibernate.dialect.SQLServerDialect

# Seguran�a JWT (L� a chave secreta do servidor)
api.security.token.secret=${JWT_SECRET}

# Configura��o do Actuator (Monitoramento seguro)
# Em produ��o, expomos menos detalhes por seguran�a, ou protegemos a rota
management.endpoints.web.exposure.include=health,info

# Logs (Apenas erros e informa��es importantes, sem debug)
logging.level.root=INFO
logging.level.com.starcard=INFO